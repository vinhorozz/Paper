export const login = (username, password) => {
    // Lógica de autenticação
    console.log(`Username: ${username}, Password: ${password}`);
    // Retorne um valor ou resultado conforme necessário
  };
  
  export const logout = () => {
    // Lógica de logout
    console.log('User logged out');
  };
  